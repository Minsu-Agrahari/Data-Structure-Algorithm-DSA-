// Time Complexity : O(n^2)

/*
Best Case Omega(n log(n)) :
    Occurs when the pivot divides the arrays into two equal halves.

Average Case Theta(n log(n)):
    Random pivot selection usually produces balanced partitions.

Worst Case O(n^2):
    Happens when the pivot is always the smallest or largest element(e.g. already sorted arrays with poor pivot choice)
 */


package Array.Sorting;
